# yaml-language-server: $schema=https://raw.githubusercontent.com/microsoft/AgentSchema/refs/heads/main/schemas/v1.0/AgentManifest.yaml
name: hosted-toolbox-auth-paths
displayName: "Hosted Toolbox - Authentication Paths"

description: >
  A hosted agent demonstrating five MCP-tool authentication paths in a single
  Foundry Toolbox: API key via project connection, Microsoft Entra agent
  identity, Microsoft Entra project managed identity, custom OAuth identity
  passthrough, and inline Authorization (anti-pattern). The toolbox itself is
  provisioned out of band; see this sample's README for the portal walkthrough.

metadata:
  tags:
    - AI Agent Hosting
    - Azure AI AgentServer
    - Responses Protocol
    - Agent Framework
    - Foundry Toolbox
    - Authentication
    - MCP

template:
  name: hosted-toolbox-auth-paths
  kind: hosted
  protocols:
    - protocol: responses
      version: 1.0.0
  resources:
    cpu: "0.25"
    memory: 0.5Gi
  environment_variables:
    - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
      value: "{{AZURE_AI_MODEL_DEPLOYMENT_NAME}}"
    - name: TOOLBOX_NAME
      value: "auth-paths-toolbox"
parameters:
  properties: []
resources:
  - kind: model
    id: gpt-4o
    name: AZURE_AI_MODEL_DEPLOYMENT_NAME
  - kind: toolbox
    name: auth-paths-toolbox
    tools: []
